# import pytest
# from apps.bs.base import Bs

# @pytest.mark.db
# def test_merge(case):
#     url = 'http://web/{}/'.format(case.id)
#     bs = Bs(url)
#     assert bs.need_merge() == case.expected['res']
